name: Unzip and Deploy

on:
  push:
   branches: 
     - main
    paths:
      - 'wheelsdrive_final.zip'

jobs:
  unzip:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Unzip project
        run: |
          unzip -o wheelsdrive_final.zip
          cp -r wheelsdrive/. .
          rm -rf wheelsdrive wheelsdrive_final.zip

      - name: Commit & Push to Repository
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add -A
          git diff --cached --quiet && echo "Nothing to commit" || git commit -m "Extract WheelsDrive complete - white theme + Sell Your Car + PWA"
          git push origin main --force
